<!DOCTYPE html>

<html lang="en" ng-app="expenseTracker">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title ng-bind="title"> @*@ViewBag.Title*@</title>

    @Styles.Render("~/Content/style")
    @Scripts.Render("~/bundles/modernizr")
</head>
<body>

    <menubar ng-controller="routeController"></menubar>
    @*<div ng-controller="routeController">
            ng-if="$location.path()==='/LogIn'?false:true"
            {{$location.path()}} {{showmenubar}}
        </div>*@
    <div class="container body-content">
        @RenderBody()
        @*<hr />
            <footer>
                <p>&copy; @DateTime.Now.Year - My ASP.NET Application</p>
            </footer>*@
    </div>

    @Scripts.Render("~/bundles/angular")
    @RenderSection("scripts", required: false)
</body>
</html>